/*Create an STL-style algorithm that is an overloaded version of for_each( )
 * which follows the second form of transform( ) and takes two input ranges so
 * it can pass the objects of the second input range a to a binary function that
 * it applies to each object of the first range.
 */

template<typename first1, typename last1, typename first2, typename last2>
void for_each() {
}

int main() {

}
